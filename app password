import smtplib

EMAIL = "sniperbytesofficial@gmail.com"
PASSWORD = "znnoodlhxdoytxij"

# Send email
with smtplib.SMTP_SSL('smtp.gmail.com', 465) as server:
    server.login(EMAIL, PASSWORD)
    server.sendmail(EMAIL, "sniperbytesofficial@example.com",
                    "Subject: Hello\n\nThis is the email body.")

print("Sent!")
